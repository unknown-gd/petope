[package]
name = "petope"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
tokio = { version = "1", features = ["rt", "net", "macros", "io-util", "time"] }
anyhow = "1.0"
bytes = "1"
mdns-sd = "0.19"
